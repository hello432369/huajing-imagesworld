{
  "name": "huajing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "tauri": "tauri",
    "build:bundle": "tauri build && plutil -replace CFBundleDisplayName -string '画境' 'src-tauri/target/release/bundle/macos/huajing.app/Contents/Info.plist' && plutil -replace CFBundleName -string '画境' 'src-tauri/target/release/bundle/macos/huajing.app/Contents/Info.plist'"
  },
  "dependencies": {
    "pinia": "^2.2.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vue-tsc": "^2.0.0"
  }
}
